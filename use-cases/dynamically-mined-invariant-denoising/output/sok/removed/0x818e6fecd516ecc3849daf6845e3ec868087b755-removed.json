[
    {
        "before_preds": [
            "ori(Sum(decimals[...])) > 0",
            "ori(Sum(decimals[...])) == 81"
        ],
        "after_preds": [
            "ori(Sum(decimals[...])) == 81"
        ]
    },
    {
        "before_preds": [
            "amount > 0",
            "amount == 40963957051"
        ],
        "after_preds": [
            "amount == 40963957051"
        ]
    },
    {
        "before_preds": [
            "Sum(decimals[...]) > 0",
            "Sum(decimals[...]) == 81"
        ],
        "after_preds": [
            "Sum(decimals[...]) == 81"
        ]
    },
    {
        "before_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])",
            "ori(Sum(decimals[...])) >= Sum(decimals[...])",
            "ori(Sum(decimals[...])) <= Sum(decimals[...])"
        ],
        "after_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])"
        ]
    },
    {
        "before_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])",
            "ori(Sum(decimals[...])) >= Sum(decimals[...])",
            "ori(Sum(decimals[...])) <= Sum(decimals[...])"
        ],
        "after_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])"
        ]
    },
    {
        "before_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])",
            "ori(Sum(decimals[...])) >= Sum(decimals[...])",
            "ori(Sum(decimals[...])) <= Sum(decimals[...])"
        ],
        "after_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])"
        ]
    },
    {
        "before_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])",
            "ori(Sum(decimals[...])) >= Sum(decimals[...])",
            "ori(Sum(decimals[...])) <= Sum(decimals[...])"
        ],
        "after_preds": [
            "ori(Sum(decimals[...])) == Sum(decimals[...])"
        ]
    }
]